import React from 'react';

const Loading = () => (
    <div className="text-center" style={{fontSize: '30px'}}><span className="glyphicon glyphicon-refresh spinning"></span></div>
);

export default Loading;
